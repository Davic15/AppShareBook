<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/background</item> //added
        <item name="android:textColor">#ffffff</item>
        <item name="android:editTextColor">@color/textColor</item>
        <item name="colorButtonNormal">@color/buttonColor</item>
        <item name="android:titleTextStyle">@color/titleColor</item>
    </style>

    <style name="ButtonColor" parent="@style/Widget.AppCompat.Button">
        <item name="android:textColor">@color/buttonTextColor</item>
    </style>

</resources>
